// Legacy entrypoint (hyphen) kept for backwards compatibility.
// The real implementation lives under `js/padre/attendance_live.js`.

export { initLiveClassListener } from './padre/attendance_live.js';
